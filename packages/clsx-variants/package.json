{
  "name": "clsx-variants",
  "version": "0.1.1",
  "description": "A tiny utility for constructing `className` variants conditionally.",
  "homepage": "https://github.com/mauricekleine/mozza",
  "repository": {
    "type": "git",
    "url": "https://github.com/mauricekleine/mozza/tree/main/packages/clsx-variants"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "clean": "rimraf dist",
    "dev": "pnpm run build -- --watch",
    "format": "prettier --write .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prepare": "npm run clean && npm run build",
    "prepublishOnly": "pnpm test && pnpm run lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "clsx": "~1.2.1",
    "rimraf": "~4.4.1",
    "tsup": "~6.7.0"
  },
  "devDependencies": {
    "eslint": "~8.37.0",
    "eslint-config-mozza": "workspace:*",
    "prettier": "~2.8.7",
    "tsconfig": "workspace:*",
    "typescript": "~5.0.3",
    "vitest": "~0.29.8"
  },
  "engines": {
    "node": "^18.0.0"
  }
}
